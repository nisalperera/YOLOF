{
  "name": "YOLOF",
  "build": {
    "context": "${localWorkspaceFolder}",
    "dockerfile": "${localWorkspaceFolder}/docker/Dockerfile",
    "args": {
      "UID": "${localEnv:USER_ID:1000}",
      "GID": "${localEnv:GROUP_ID:1000}",
      "USER": "${localEnv:USER:ubuntu}"
    }
  },
  "workspaceFolder": "/home/${localEnv:USER}/YOLOF",
  "remoteUser": "${localEnv:USER}",
  "updateRemoteUserUID": false,
  "containerEnv": {
    "HOME": "/home/${localEnv:USER}",
    "USER": "${localEnv:USER}"
  },
  "runArgs": [
    "--network=host",
    "--ipc=host",
    "--gpus=all",
    "--env-file=${localWorkspaceFolder}/.env",
    "--name=YOLOF"
  ],
  "mounts": [
    "source=${localEnv:HOME}/Projects/personal/data/,target=/home/${localEnv:USER:ubuntu}/YOLOF/datasets/,type=bind",
    "source=${localEnv:HOME}/Projects/personal/models/,target=/home/${localEnv:USER:ubuntu}/YOLOF/models/,type=bind",
    "source=${localEnv:HOME}/Projects/personal/output/,target=/home/${localEnv:USER:ubuntu}/YOLOF/output/,type=bind"
    
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-python.debugpy",
        "njpwerner.autodocstring"
      ]
    }
  },
  "remoteEnv": {
    "DOCKER_BUILDKIT": "0"
  }
}