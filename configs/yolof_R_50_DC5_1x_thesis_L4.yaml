_BASE_: "yolof_R_50_DC5_1x_thesis_base.yaml"

SOLVER:
  IMS_PER_BATCH: 8          # local default (overridden on cloud)
  MAX_ITER: 10           # ~8 epochs if 1x ~90000, adjust to your schedule
  WARMUP_ITERS: 1
  STEPS: [0.66, 0.88]     # ≈ epochs 5 and 7 in an 8-epoch run
  GAMMA: 0.1

LOGGER:
  WANDB:
    NOTES: "L4: Fine-tuning YOLOF R-50 DC5 for thesis experiments. Adjusted Max Iterations."
    NAME: L4

SEED: 314
OUTPUT_DIR: "output/finetune_thesis_L4"